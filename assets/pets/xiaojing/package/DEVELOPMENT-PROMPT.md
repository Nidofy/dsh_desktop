# 复制到开发线程的完整 prompt：0.2.1～0.2.3

请在 DSH Desktop 项目 `D:\Documents\FeiyangChen\devApps\dshWinUi` 中完成覆盖 0.2.1、0.2.2、0.2.3 的桌面宠物开发。第一版宠物正式名称为「吃白饭的大肥鱼」。0.2.0 正式版已准备好，本任务在此基线上分三个里程碑持续实现、测试和交付，不仅提供方案，也不在完成 0.2.1 后结束整个任务。不覆盖现有 0.2.0 发布目录，具体构建编号按项目发布流程决定。

先读取：

1. 当前工作区适用的 AGENTS.md（如有）、README.md、docs/RELEASE-0.2.0.md、docs/evidence/stable-0.2.0.json、docs/BUILD.md。
2. `assets/pets/xiaojing/package/INTEGRATION.md`，这是覆盖 0.2.1～0.2.3 的详细交接说明，含阶段范围、素材依赖、事件映射、代码入口、兼容性、迁移与验收要求。必须完整阅读，不能只实施 MVP 部分。
3. 同目录 README.md、pet.json、asset-manifest.json、atlas-layout.json、checksums.json；需要看动作时打开 preview.html。

保留资源内部 ID `xiaojing`，所有界面使用显示名称「吃白饭的大肥鱼」。现成基础图集已经完成 9 组动画、16 个观察方向和透明母版，1536×2288、8×11、192×208/格、spriteVersionNumber 2。按 asset-manifest.json 的逐帧时长播放，使用指定帧数；行 0 列 6 是中性观察格，不是第七帧待机。保留已通过检查的基础图片；0.2.2 新增互动素材另做扩展，不重生成整套基础图集，不安装到用户级 Codex pets。

先确认工作树：本目录有大量 0.2.0 现有修改与未跟踪文件，资源也可能未提交。不得 reset/clean、丢弃已有修改、擅自提交全部工作树，或从旧 HEAD 新建工作树后漏掉当前正式版功能。若使用隔离工作树，必须带齐实际 0.2.0 基线与该资源包，并验证差异。

按以下三个阶段完成，详细边界以 INTEGRATION.md 为准：

**0.2.1 — 桌宠接入。** 默认关闭的单只宠物：小型透明置顶本地 Tauri 窗口、启停设置、拖动与位置恢复、任务自动跟随与固定会话、状态气泡、点击气泡/双击宠物定位会话、单击短暂挥手、退出清理。完成有界只读状态适配、快照/增量、资源 staging 和离线交付。

**0.2.2 — 互动完善。** 完整 Pointer Events 手势识别，支持 mouse/touch/pen 的单击、双击、长按、拖动和取消；头部/身体互动、16 方向 hover 观察、反应优先级、选择器、缩放与互动偏好。补充 pat、happy、dragged 三项独立透明动画及声明式 interactions.json；基础图集不变。使用本包母版和原图保持角色一致，交付图像、时长、预览、校验和视觉记录。缺素材时可用明确标记的基础动作回退联调，但不能宣称新增美术或 0.2.2 完整验收已完成。若没有图像工具，先完成所有不依赖新图的工作与素材需求单，清楚保留该依赖，不制作假素材。

**0.2.3 — 资源与多宠物扩展。** 本地目录/ZIP 导入、预检、选择、替换、删除与导出；经样例确认的 8×9 标准动作及 8×11/v2 包兼容子集；声明式 DSH interactions 与缺失能力回退；最多 3 个实例、每实例独立位置/缩放/固定目标，共享事件来源，自动分配任务且提醒去重；资源声明的随机待机；可关闭/重置的本地轻量心情与亲密度；纯文本气泡个性化；0.2.1→0.2.2→0.2.3 配置迁移及损坏恢复。不要宣称兼容所有 Codex 格式，核对上游定义并保存兼容样例。限制包大小/文件数/解码像素，拒绝路径逃逸、链接、重解析点、重复项、压缩炸弹；只验证和加载数据，不执行包内脚本。内置资源可恢复、导入原子安装、失败不损坏原包，导出不带会话和连接信息。

三阶段共同要求：任务状态、事件短暂反应、用户互动和心情分开。等待信息不能被摸/拖/心情清掉；完成只表示本轮结束，不表示测试通过；取消静默；review 没有原生结构化信号时不自动推断。心情只影响空闲表现，不驱动模型，不进行复杂养成或离线惩罚。逐像素穿透、全局鼠标钩子、联网商店、脚本插件框架和模型驱动对话不在 0.2.3 范围内。

宠物只能消费原生结构化任务事件与最小状态视图。优先利用 task-recovery 的状态语义、session/event 来源和现有宿主桥接；NotificationFeed 只有有限时效提醒且受通知开关影响，不能当持续任务状态源。处理多会话、去重乱序、快照/增量竞态、重启/连接切换、旧点击、冷恢复不重播事件。若抽取通知与宠物共用层，保证现有通知语义不变。notifications.rs 当前检查所有窗口是否在前台，新增 pet 必须避免宠物焦点抑制正常后台提醒。

严禁影响 prompt、tools、session history、模型请求、cache key/prefix；不加模型调用，不发送聊天或代批权限。宠物窗口单独最小权限，本地资源与生产 CSP 保持约束；preview.html 是检查工具，不直接当生产页面。默认气泡使用固定简短文案，不传完整恢复记录、标题/路径、命令或模型输出。资源缺失/宠物故障不得阻断主程序。

参考本地 `D:\Documents\FeiyangChen\Game\virtualPet` 的状态调度、动画与交互思路；只读参考，不移植 WPF、hooks、模型链或旧养成系统。0.2.3 的轻量心情按交接说明独立实现。注意旧项目的 desktop 版本常量和 FPS 不适用本包，具体文件见交接说明。

实施顺序：0.2.1 先验证资产及打包后的本地窗口，再做只读状态适配与纯状态机，再接设置/基础交互/导航，最后做协议回归、实际 Windows 窗口验收和离线目录检查。随后完成 0.2.2，再完成 0.2.3；每阶段保存变更、测试、运行证据、资源依赖状态与已知限制，不要把后续阶段永久留为 TODO。局部素材或硬件依赖阻塞时推进其余不依赖部分，不绕过门禁宣称完整通过；无需为推进已授权阶段重复请求确认。

增加确定性资源 staging，确保 assets 下图集实际进入交付包；不要直接修改 dist 或 runtime 生成结果。利用现有构建/测试流程，新增与变化相关的有意义测试，使用可注入时钟/随机源，输出实际执行结果。

共同验收等待期间交互、多任务跟随、取消/子任务/冷读、旧连接回调、通知与宠物独立开关、宠物前台不吞通知、双协议请求与缓存前缀不变、DPI/多屏拖动恢复、关闭/退出/隐藏性能、离线资源完整性。0.2.2 追加手势冲突/取消、方向防抖、互动返回最新任务状态、缩放与新增动画检查。0.2.3 追加兼容样例、恶意/损坏包拒绝、缺失扩展回退、多实例任务分配/去重、1 只和 3 只性能、心情持久化、模板转义、配置升级及恢复测试。不能把静态图集检查或浏览器预览当成 DSH 原生窗口验收；未实测项明确列出。

请直接开展实现，持续推进上述三个阶段。交付时分别列出 0.2.1/0.2.2/0.2.3 的完成状态、修改内容、可运行交付位置、已执行检查和剩余限制，区分功能完成、素材完成和现场验收。不要自动推送或发布，不要更改用户级 Codex 配置。
