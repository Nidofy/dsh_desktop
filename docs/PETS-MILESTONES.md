# 桌宠连续里程碑记录

基线：2399b3d（0.2.0），开工时仅 assets/pets 未跟踪。保留实际 0.2.0 工作区基线与已批准资源，没有 reset/clean、提交、推送或覆盖正式版目录。首只宠物显示名称为「吃白饭的大肥鱼」，内部 ID 为 xiaojing。

## 0.2.1-rc.1：桌宠接入

功能：默认关闭；小型透明置顶本地窗口；按清单帧数和逐帧时长播放；有界 task-recovery 状态适配；自动/固定会话、固定短文案气泡、定位会话、单击挥手、拖动与位置恢复；通知前台判断排除 pet 窗口；独立权限与确定性资源 staging。

- 目录：dist/DSHDesktop-0.2.1-rc.1-win-x64/DSHDesktop.exe。
- 构建 PASS：.build/pet-021-gate.log；buildId e480a870-6a35-4160-a9d1-b1d7c7853c5d。
- 离线目录完整性 PASS：26814 文件 / 1046140903 字节；.build/pet-021-package.log。
- 原生进程 PASS：.build/desktop-process-b06ae11396de44328329cfe9b690c652/report.json。实际本地图集解码、启动 6446ms、随包 WebView2、loopback、单实例、后端异常报告与退出清理。
- 5 项新增纯逻辑测试通过，原有通知与双协议/缓存前缀回归通过。素材使用原始 v2 图集，未重生成。
- 现场视觉、真实触控/笔、DPI/多屏、通知焦点体验未全部验收；此目录为候选版。

## 0.2.2-rc.1：互动完善

功能：统一 Pointer Events，mouse/touch/pen 的单击、双击、长按、拖动和取消；头部/身体互动；16 方向观察及防抖；反应优先级；资源选择结构、75–200% 缩放与互动偏好；配置迁移备份。

- 目录：dist/DSHDesktop-0.2.2-rc.1-win-x64/DSHDesktop.exe。
- 构建 PASS：.build/pet-022-gate.log；buildId 4df3a6a5-69a6-4f93-9867-4f0b9b987aae。
- 离线目录完整性 PASS：26816 文件 / 1046574356 字节；.build/pet-022-package.log。
- 原生进程 PASS：.build/desktop-process-4fafc73380684f7db36d553facd17dec/report.json。基础和三项扩展解码后才发送 ready；启动 5463ms，生命周期检查通过。
- 11 项宠物逻辑检查、70 项 Rust 单元检查、独立原生快照检查及 38 项双协议前缀用例通过。
- 独立新美术：pat 4 帧、happy 4 帧、dragged 3 帧；透明 PNG/WebP、逐帧时长、校验、正常尺寸 contact sheet 和动画预览位于 assets/pets/xiaojing/extensions。基础 atlas 原校验不变。镜像配饰和跨格生成帧被排除并记录，未用基础动作冒充新美术。
- 素材接入与静态视觉检查完成；真实手势、DPI/多屏和连续动画的全部现场验收仍单列，不由模拟事件代替。

## 0.2.3：资源与多宠物扩展（当前 RC2）

功能实现：本地目录/ZIP 导入预检与预览、显式同 ID 替换、选择/删除/导出、内置恢复；经保存样例检验的 8×9 和 8×11/v2 子集；声明式互动与缺失能力回退；最多三实例独立位置/缩放/固定目标；共享有界快照/增量、任务分配和气泡去重；随机待机；可关闭/重置的本地心情和亲密度；纯文本气泡与 1→2→3 迁移、损坏恢复。

设置合并使用宿主锁，只更新设置窗口实际修改字段，不覆盖原生拖动位置、隐藏状态或新亲密度。气泡尺寸按实际文本和缩放计算，限制高度。资源失败回退内置包，加载失败不阻断主程序。

RC1 完整构建 PASS：.build/pet-023-final-gate.log；buildId a337f75e-a1a1-473c-a21c-e39eb52bf9ec。17 项宠物 JavaScript 检查、资源实际解码/坏包/生命周期检查、80 项 Rust 单元检查及另行执行的原生快照检查通过；38 项双协议前缀用例通过。宠物关闭/启用两种状态下，OpenAI 与 Anthropic 最终请求字节一致，通知开关独立及快照/增量检查通过。证据 .build/observability-wire-qlZDdr。

交付目录完整性与后置原生检查结果将追加至仓库 docs/evidence/pets-0.2.3.json。随包文档为打包时快照，后置现场检查以仓库更新的记录为准。

## 所有阶段的现场边界

静态图集、模拟手势、原生进程 ready 分属不同证据，不代表全部现场体验通过。真实触屏/笔、100/150/200% DPI、多屏负坐标和拔屏、焦点下系统通知送达、长期运行、隐藏后的系统唤醒频率需继续现场检验。没有使用全局鼠标钩子、逐像素穿透、联网商店、包内脚本执行、模型驱动对话或用户级 Codex pets 安装。

兼容范围见 pets/compatibility/README.md；后续版本不永久停在 MVP。0.2.3 的当前状态取代历史暂停点 docs/pets/PAUSED-2026-09-22.md。

## 0.2.3 RC2 原生窗口修正

RC1 后置窗口检查发现继承的应用菜单造成额外高度，RC2 已移除该菜单。Tauri/tao 原生窗口保留 WS_CAPTION 样式位，但使用 WM_NCCALCSIZE 去除装饰；现改以实际菜单句柄、客户区和外框尺寸判定。

隔离修正探针 .build/pet-native-inspect-55192a6093334ba8bf5f866ecc4c98b2/report.json：175% DPI，三个窗口均可见/置顶、无菜单，客户区和外框均为 420×364；导入 8×9 样例没有触发回退气泡；关闭一个实例后剩余两个且配置同步。该探针先于最终打包，最终交付版仍另行运行原生检查。

取消边界已追加回归：任务反应在取消或跟随目标改变时立即结束；用户互动与持续等待仍独立。当前宠物专项共 18 项。RC2 前一轮门禁期间加入此修正，发布回执因输入变化被拒绝（.build/pet-023-rc2-gate.log）；没有绕过该门禁，冻结源码后重新执行 .build/pet-023-rc2-final-gate.log。

## 0.2.3-rc.2 最终交付

- 完整门禁 PASS：.build/pet-023-rc2-final-gate.log；buildId 910e369d-212f-491a-b7f6-8ce8b101b4e4。18 项宠物专项、80 项 Rust 及独立原生快照、双协议请求字节一致和 38 项前缀用例通过。
- 可运行目录：D:\Documents\FeiyangChen\devApps\dshWinUi\dist\DSHDesktop-0.2.3-rc.2-win-x64；离线完整性日志 .build/pet-023-rc2-package.log。未覆盖旧候选或 0.2.0。
- 最终交付版的 0/1/3 只原生进程检查 PASS：随包 WebView2、loopback、单实例、后端故障报告、退出清理。三窗口无菜单且客户区等于外框；175% DPI 下每只 420×364 物理像素；关闭一只保留其余两只并保存设置。导入 8×9 资源实际加载，无缺失扩展错误。
- 原生渲染 PNG、窗口属性、透明采样与进程记录已保存至 docs/pets/evidence/0.2.3-rc.2。透明边缘测试使用已知白色底板，另查角色中心不为白色，避免被底板遮住时误判透明；具体结果以 transparency.json 为准。

| 宠物数 | 单核 CPU 百分比 | 进程树专用内存 MiB | 采样秒数 |
|---|---:|---:|---:|
| 0 | 3.91 | 664.2 | 10.01 |
| 1 | 4.83 | 876.3 | 10.02 |
| 3 | 14.35 | 943.5 | 10.01 |

环境逻辑处理器数 16。CPU 为一个逻辑核满载=100%的口径，内存包含整个 DSHDesktop/引擎/WebView2 进程树，不是宠物单独占用。10 秒启动后样本用于记录，不代表长期稳态；系统唤醒、真实触控/笔、混合 DPI/拔屏和通知焦点现场项仍未全部测量。

最终原生截图三张已逐张检查：角色完整，无裁切或菜单空白带。PrintWindow 截图的黑色底不用于判定透明，独立白色底板的三组透明边缘与非白角色中心采样均 PASS。此前 0.2.1/0.2.2 候选目录保留用于阶段追溯，当前体验请使用累计修正后的 0.2.3-rc.2。
