# 用户请求暂停：2026-09-22

此记录为历史暂停点。用户已发送“继续”，任务已恢复。没有自动提交、推送或发布；未覆盖 0.2.0 交付目录。

## 已有交付

- 0.2.1-rc.1：`dist/DSHDesktop-0.2.1-rc.1-win-x64/DSHDesktop.exe`。完整构建、目录完整性和隔离原生启动通过。
- 0.2.2-rc.1：`dist/DSHDesktop-0.2.2-rc.1-win-x64/DSHDesktop.exe`。完整构建、目录完整性、基础及独立互动资源在原生窗口解码通过。
- 基础图集保持原校验；pat/happy/dragged 独立素材及视觉记录在 `assets/pets/xiaojing/extensions/`。
- 详细构建编号和既有证据见 `docs/PETS-MILESTONES.md`。真实触控、笔、DPI/多屏和通知焦点体验未完成现场验收。

## 0.2.3 当前状态

源码版本为 0.2.3-rc.1，尚未进行完整构建、打包或原生运行验收。实现中的内容包括本地目录/ZIP 预检与原子安装、资源选择/替换/删除/导出、最多三个实例、共享快照/增量事件、任务分配和提醒去重、配置迁移与恢复、轻量心情和纯文本气泡。

已实际通过：15 项 JavaScript 桌宠测试，资源包校验及生命周期测试，9 项桌宠相关 Rust 测试。最近 Rust 测试完成于气泡尺寸接口最后一次修改之前；该最后修改尚未编译验证。没有正在运行的构建或测试进程，暂停时未发现 DSHDesktop 进程。

## 继续时的工作

1. 先检查工作树，保留当前全部改动；不 reset/clean、不从旧 HEAD 丢失实际基线。
2. 编译并测试最后的 `bubble-size` 前后端接口：按实际气泡高度缩放、限制高度、避免长文本裁切。审核 `shell-ui/pet/pet.js`、`src-tauri/src/pets.rs`。
3. 修复设置页旧快照可能覆盖原生拖动位置或亲密度的问题：保存时只合并用户实际修改字段；当前已加并发保存排队，但未解决跨窗口旧值覆盖。
4. 检查包索引损坏恢复、原子安装失败清理、同 ID 替换热加载及配置迁移备份。已完成基础坏包和失败替换测试，尚需最终审查。
5. 运行新增双协议最终请求字节对比（`tests/observability-wire.mjs` 已加入宠物开关/通知独立/增量检查，尚未执行），确认 prompt/tools/history/cache prefix 不变。
6. 完成独立互动预览说明、兼容样例来源和清单文档。8×9 兼容样例保存在 `tests/fixtures/pets/standard-8x9`，是由现有角色裁切形成的布局测试样例，不代表全部 Codex 格式。
7. 执行项目完整构建门禁，生成独立 0.2.3 候选目录。构建回执开始后不要修改其输入文件。
8. 执行 `tests/desktop-process-smoke.ps1 -PackagePath ... -PetCount 0/1/3 -MeasureIdle`：新加入三实例/CPU/内存采样支持，尚未执行；真实 DPI/多屏/触控/笔及视觉验收需明确区分。
9. 更新各阶段证据与交付文档，区分功能、素材和现场验收。`PETS-MILESTONES.md` 的早期“0.2.3 尚未开始”条目已过时，以本暂停记录为当前状态。

现有授权是持续完成三个阶段，但当前用户明确要求暂停，收到继续命令后才恢复。
